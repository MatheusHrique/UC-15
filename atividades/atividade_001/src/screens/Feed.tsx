import { SafeAreaView, ScrollView, View } from "react-native";
import Card from "../components/Card";

const Feed = () => {
  return (
    <View>
      <Card />
    </View>
  );
};

export default Feed;
